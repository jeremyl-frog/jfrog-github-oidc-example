{
  "name": "oidc-npm-publish-example",
  "version": "1.0.1",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm pack"
  },
  "author": "Jeremy Leopold",
  "description": "oidc package deployment example",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.2"
  }
}
